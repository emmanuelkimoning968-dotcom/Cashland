import React from "react";
import ReactDOM from "react-dom/client";
import Cashland from "./Cashland.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Cashland />
  </React.StrictMode>
);
